import 'package:flutter/material.dart';

class NoProductsDisplay extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: Implement.
    return Center(
      child: Text("No products in store"),
    );
  }
}
