import 'package:flutter/material.dart';

class UploadItemScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    print("Building upload");
    return Scaffold(
      appBar: AppBar(
        title: Text("Upload Screen"),
      ),
      body: Center(
        child: Text("Upload Screen"),
      ),
    );
  }
}
